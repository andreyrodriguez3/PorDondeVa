package com.tubus.driver.ui.theme

import androidx.compose.material3.Typography

// Default M3 type scale — the driver app's screens are a handful of status readouts and
// two buttons, not dense text content, so the stock scale already fits; the brand
// identity here lives in color (Color.kt), not a custom type ramp.
val TuBusTypography = Typography()
