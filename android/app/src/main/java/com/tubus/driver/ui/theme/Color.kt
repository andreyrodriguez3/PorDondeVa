package com.tubus.driver.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors apps/web's brand blue and semantic tokens (tailwind.config.js / globals.css)
// so the driver app reads as the same product as the admin/passenger web surfaces,
// rather than a bare, unbranded Material 3 default.
val Brand = Color(0xFF3D5AFE)
val BrandDark = Color(0xFF7D8FFF)

val Danger = Color(0xFFE0483E)
val DangerDark = Color(0xFFF2665C)

val LightBackground = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFF5F6F8)
val LightOnBackground = Color(0xFF0F1115)
val LightOutline = Color(0xFFE4E6EA)

val DarkBackground = Color(0xFF121418)
val DarkSurfaceVariant = Color(0xFF23262E)
val DarkOnBackground = Color(0xFFEDEEF1)
val DarkOutline = Color(0xFF2A2D36)
